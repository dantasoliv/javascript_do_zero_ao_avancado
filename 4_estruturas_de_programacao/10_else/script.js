let velocidade = 100;

if (velocidade <= 80) { // Bloco IF é executado quando a condição retorna true
    console.log("Não foi multado");
} else { // Bloco else é executado quando a condição retorna false
    console.log("Foi multado");
}