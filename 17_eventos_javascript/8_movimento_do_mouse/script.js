window.addEventListener("mousemove", function(e){ // Adicionando o evento mousemove
    console.log(e.x); // Acessando o valor do exixo x da posição do mouse
    console.log(e.y); // Acessando o valor do exixo y da posição do mouse
});