let nome = "Daniel";
console.log(nome.length); // A propriedade length mostra a quantidade de caracteres um uma string

let numeros = [1,2,3,4,5,6,7,8];
console.log(numeros.length); // Em uma Array a proriedade length mosra a quantidade de elementos
console.log(numeros['length']); // Outra forma de usar a propriedade length

let nomes = ["Daniel","Marcos","Maria"];
console.log(nomes.length);
console.log(nomes[1]); // [1] é uma propriedade para acessar um elemento de uma array pelo valor do indíce